<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp"
    android:background="@color/white">

    <!-- Toolbar -->
    <androidx.appcompat.widget.Toolbar
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#000000"
        android:theme="@style/ThemeOverlay.AppCompat.Dark.ActionBar"
        android:layout_gravity="center"
        android:title="Available Showtimes"
        android:titleTextColor="@android:color/white"
        android:titleTextAppearance="@android:style/TextAppearance.DeviceDefault" />

    <!-- Theater Name -->
    <TextView
        android:id="@+id/theaterNameTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="20sp"
        android:textStyle="bold"
        android:layout_marginTop="16dp"
        android:text="Theater Name" /> <!-- Add a default text or set it dynamically -->

    <!-- Movie Title -->
    <TextView
        android:id="@+id/movieTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="16sp"
        android:textColor="#555"
        android:layout_marginTop="8dp"
        android:text="Movie Title" /> <!-- Add a default text or set it dynamically -->

    <!-- Showtimes RecyclerView -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerViewShowtimes"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="16dp"
        android:layout_weight="1" /> <!-- Adjusted to take up remaining space in layout -->

</LinearLayout>
